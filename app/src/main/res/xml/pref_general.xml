<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="pref_key_location_updates"
        android:title="@string/pref_title_location_updates"
        android:summary="@string/pref_description_location_updates"
        android:defaultValue="true" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <!--<EditTextPreference-->
        <!--android:key="example_text"-->
        <!--android:title="@string/pref_title_display_name"-->
        <!--android:defaultValue="@string/pref_default_display_name"-->
        <!--android:selectAllOnFocus="true"-->
        <!--android:inputType="textCapWords"-->
        <!--android:capitalize="words"-->
        <!--android:singleLine="true"-->
        <!--android:maxLines="1" />-->


    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
<!--    <ListPrefer-->ence
    <!--    android:key="example_l-->ist"
    <!--    android:title="@string/pref_title_add_friends_to_messa-->ges"
    <!--    android:defaultValue=-->"-1"
    <!--    android:entries="@array/pref_example_list_tit-->les"
    <!--    android:entryValues="@array/pref_example_list_val-->ues"
    <!--    android:negativeButtonText="@n-->ull"
    <!--    android:positiveButtonText="@null-->" />

</PreferenceScreen>
